# setup.py
from setuptools import find_packages, setup

setup(
    name='clib',
    packages=find_packages(),
    version='0.1.1',
    description='a compression library made in pure Python',
    author='YourLocalMoon',
)